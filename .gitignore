# Never commit real private CV data or the keys that unlock it.
/private/private.json
/.keys/

# Tailscale-issued dev HTTPS cert/key — machine-specific, regenerate with `npm run cert`.
/.certs/

node_modules/
dist/
.astro/
.DS_Store

# playwright-cli scratch
.playwright-cli/
